[package]
name = "test-server-gpu-api"
version = "0.1.0"
edition = "2024"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]

log = "*"
env_logger = "*"
tokio = { version = "*", features = ["rt-multi-thread"] }
axum = "*"
tower-http = { version = "*", features = ["fs"] }
